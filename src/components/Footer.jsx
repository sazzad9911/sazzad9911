import React from 'react';
import {animate,useSpring} from 'react-spring';
const Footer = () => {
    const styles = {
        footer: {
            backgroundColor:'#154360',
            width:'100%',
            minHeight:'300px',
        }
    }
    return (
        <div style={styles.footer}>
            
        </div>
    );
};

export default Footer;